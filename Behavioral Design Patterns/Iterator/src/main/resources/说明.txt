迭代器的几个要素：

1、抽象容器：包含新增，删除元素等管理元素的方法，生产容器的迭代器等
2、具体容器：实现抽象容器的方法，并包含一个集合，容器元素存储在改集合中
3、抽象迭代器：java.util.Iterator
4、具体迭代器：包含一个集合，并实现Iterator的方法，一般有hasNext(),getNext() 的方法

迭代器迭代的集合就是容器的集合，容器在生产迭代器对象时，把自身的集合转交给迭代器去迭代